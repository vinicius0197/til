# Callback of map function
The callback of the `map` function accepts three arguments:
- current item value
- curret item index
- the array `map` was called upon

```javascript
  array.map((value, index, array) => {
    // do something
  })
```
